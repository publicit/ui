version: '3.8'
services:
  app:
    image: my-app
    build:
      context: ./
      target: dev
      dockerfile: docker/dev.Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
    ports:
      - "3000:3000"
